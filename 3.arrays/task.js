function compareArrays(arr1, arr2) {
    let result;
 
    result = (arr1.length === arr2.length) && (arr1.every((elem, index) === arr2[i]));
    
     return result;
}

function getUsersNamesInAgeRange(users, gender) {
    return arr.filter(item => item > 0 && item % 3 === 0).map(item => item *10);
  
}